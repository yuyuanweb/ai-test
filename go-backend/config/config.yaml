server:
  port: 8080
  mode: debug  # debug/release

log:
  dir: logs
  level: info  # debug/info/warn/error
  maxAge: 30  # 日志保留天数
  compress: false  # 是否压缩

database:
  host: localhost
  port: 3306
  user: root
  password: 11111111
  dbname: ai_eval
  charset: utf8mb4
  parseTime: true
  loc: Local
  maxIdleConns: 10
  maxOpenConns: 100

redis:
  addr: localhost:6379
  password: "111111"
  db: 1
  poolSize: 10

session:
  secret: yupi
  maxAge: 2592000  # 30天（与Java保持一致）
  cookieName: sessionId

openrouter:
  apiKey: "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"
  baseURL: "https://openrouter.ai/api/v1"

rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
