# 本地开发环境配置
spring:
  # MySQL 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_eval
    username: root
    password: 11111111
  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password:
